require 'spec_helper'

describe Brew::Gem do
  it 'has a version number' do
    expect(Brew::Gem::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
